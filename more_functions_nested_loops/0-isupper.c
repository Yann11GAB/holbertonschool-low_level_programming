#include "main.h"
/**
  * _isupper - Check if c is an uppercase character (between 'A' and 'Z')
  * @c: c is a number
  * Return: 1 if c is uppercase, 0 otherwise
  */
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
